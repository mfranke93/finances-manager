/**
 * Should only be defined once.
 * This signals to Catch that it should provide its own main() method for
 * tests. This file does not contain any tests. Instead, it is preferrable to
 * have one file per class for which the test cases are written. Naming
 * convention for a class <code>foo.[ch]pp</code> should be
 * <code>test_foo.cpp</code>.
 */
#define CATCH_CONFIG_MAIN
#include "../third_party/catch/catch.hpp"

